#include <bits/stdc++.h>

using namespace std;

int main(){
    int t;
    cin >> t;
    while(t--){
        int n,m;
        cin >> n >> m;
        int a[n + 1][m + 1];
        for(int i = 1;i <= n;i++){
            for(int j = 1;j <= m;j++){
                cin >> a[i][j];
            }
        }
        int i = 1,j = 1;
        bool check[n + 1];
        queue<int> qu;
        qu.push(1);
        check[1] = true;
        while(!qu.empty()){
            
        }

        
    }
    return 0;
}
